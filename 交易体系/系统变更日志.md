# 系统变更日志

> 目的：让"边走边优化"可复盘。每改一处就记一条，月末按版本分段看胜率，而不是一锅烩。
> 配套：`trade_log.py buy` 会自动给每笔交易打 `CURRENT_SYSVER`（脚本顶部常量）。**改了逻辑就 bump 这个常量，并在这里记一条。**

---

## 记录格式（每条4要素）

```
### vN  YYYY-MM-DD  一句话标题
- 改了什么：具体到文件/函数/阈值（旧值 → 新值）
- 为什么改：触发它的那个观察（哪笔交易/哪天盘面/复现结果）
- 期望：这条改动应该带来什么（更多/更少候选、少踩哪种坑、盈亏比变化）
- 类型：[Bug修复] 立即上 ／ [放松风控] 慎重·最好影子先跑几天
```

## 两档速度（决定该不该立即上）

- **[Bug修复]**：本来就没按设计工作（如 S4 死代码、NaN 换手绕过市值过滤、止损倒挂）。**立即改**，不叫"改策略"。
- **[放松风控]**：松高潮×0.5、松追高门槛、放宽止损、放宽评分门槛等**降低下行保护**的调参。**最容易被最近几天行情骗、在顶部爆掉**。要求：先历史复现证明是真问题 → 最好影子跑几天 → 再正式上。
- **[加严/中性]**：收紧门槛、加排除项、纯展示/记录类。风险低，可较快上。

## 月末怎么用

```
python 工具脚本/50_交易记录/trade_log.py calibration --by version    # 分版本看胜率/盈亏/破纪律
python 工具脚本/50_交易记录/trade_log.py calibration --by strategy
python 工具脚本/50_交易记录/trade_log.py calibration --by mode
```
对比"改动前版本"和"改动后版本"的表现，用你自己的实盘数据决定：这条优化该保留、回滚、还是继续调。

---

# 变更记录（最新在上）

### 研究记录 + 展示类改动  2026-07-03  进/出场时机回测 → screener 加"尾盘执行提醒"
- 触发：用户两条实盘观察——①"等回踩往往等不到，拿到就买常是最低"；②"次日早盘开盘10分钟内冲高，账户当天最多，能不能卖"。
- 测法：`scratchpad/backtest_pullback.py`(进场) + `backtest_exit.py`/`backtest_limitsell.py`(出场)。忠实重建 S4 自身K线筛选(结构/chg5/pctChg/turn/贴MA5/收盘位/梯队/mcap)，**略去 as-of 行业强势档闸门**(候选池略宽于实盘)；D0收盘为即时买价，行情档=3指数收盘>MA20计数(强/中/弱)。样本 N=21901(等回踩区6984/即时区14917)，区间2025-07~2026-07。
- 进场结论：等回踩区**回踩成交率70%**(30%踏空)、**次日91%跌破即时买价**(→"买了就是最低"是错觉)。等回踩净D2(踏空计0) **+0.16%** vs 即时买 **−0.08%**，各档等回踩≥即时；强市近乎平手(+0.25 vs +0.16)，震荡/弱市等回踩明显胜(差0.6~0.7%)。踏空的30%即时买D2均 **+3.43%**(飞龙确实在这批)。
- 出场结论：D1盘中最高均 **+2.37%**(冲高真实)，但开盘价均 **−0.10%**、高开仅36%(→冲高不在开盘瞬间)。卖D1开盘 vs 持D2：**强市持D2更优**(+0.20% vs 开盘−0.06)、**震荡/弱市早盘卖更优**(差+0.13~0.53%)。早盘挂+2~2.5%限价成交率~40%、成交均价+2.2%。
- 改了什么：`offline_screener.py` 末尾加【尾盘执行提醒】块(按 MODE['mode'] 分强市/震荡)——进场(可买直接买/等回踩强市分批·弱市严格等回踩)+ 次日早盘出场(先挂+2~2.5%限价卖半仓；强市剩半移动止盈到D2/弱市剩半早盘清)。**纯展示提醒，不改候选/评分/风控/仓位，候选输出一字不变。**
- 类型：[加严/中性·展示类] 立即上，**不 bump sysver**(选股逻辑无变化)。回测是超集入场+代理行情档+无分笔数据，绝对数别当实盘；影子观察1个月，用 `calibration --by mode` 校准早盘卖档位是否成立。

### 事故+修复  2026-07-02  refresh 误删189只有效股 → 已恢复 + 加删除护栏
- 发生了什么：补全数据当天，`kline_daily`/`stock_industry` 有 189 只**有效**沪主板股(sh.600160~600195、sh.601150~601999 连续块，含中国银行/建设银行/兴业银行/巨化股份等)被整段删除(−67229行)。根因：`_refresh_stock_universe.py` 的 `fetch_tencent_universe` 对失败批次**静默 continue**，某次 refresh 撞上腾讯丢2~3批 → 这些有效股缺席 universe → `delete_stale_codes` 当退市删，且**无任何安全下限**。「连续代码块」=整批次丢失的典型特征。
- 恢复：写 `_restore_deleted_stocks.py`(只增不删+安全断言) 用 sina 补回 189 只×500日 OHLCV；腾讯当日快照补 07-02 真实 turn/amount。复查 distinct code 回 5208、07-02 个股回 5193、refresh 演练 库内=universe=5205/待删0。
- 改了什么(护栏)：`_refresh_stock_universe.py`——① 抓取加1次重试并统计 `_failed_batches`；② 删除前双重体检：有失败批次 / universe<4800 / 待删>50 任一命中即**拦截删除**(新股补充照常，只增不删安全)。
- 遗留(源不稳，待补)：189 只**历史 turn=0**(sina无换手，仅今日真实)、**行业分类为空**(国标A01/B06口径，需 baostock/东财在其响应时段补)。baostock收盘后登录挂死、akshare东财限流，故当日未补。
- 类型：[Bug修复] 立即上。不碰选股/价位逻辑，不 bump sysver。

### 研究记录（非改动·决策留底）  2026-07-02  S4退出「破MA5 vs 到D2」回测 → 维持D2
- 问题：用户提出 D2 硬清仓太死，能否改成"持到破MA5"的趋势型退出让利润奔跑。
- 测法：`scratchpad/bt_s4_exit.py`，S4超集入场(缺概念/龙头两层)10966笔配对比——A(到D2) / B(持到破MA5,D8兜底) / C(赢家才延:D2盈利≥2%且收MA5上才续持)。三者共用下行(破MA5收盘 + 盘中-6%熔断)。
- 结果：未封顶 C 看似最好(均-0.07% vs A-0.13%)；但 **winsorize 封顶@+30%/20% 后 B/C 全面劣于 A**(C -0.26%/-0.47% vs A 稳定 -0.13%)→ C/B 的优势 100% 来自极少数 +50~97% 怪兽单，不稳。A 的胜率(42% vs B29%/C33%)、波动、持有天数、稳健性全面最优；A 期望几乎不随封顶变化=可重复，B/C=靠运气抓龙头。
- 结论：**[否决] 维持 S4 现行 D2 清仓。** "让利润奔跑"在稳健口径下反而更差。现有 D3 豁免(盈利>2%+板块领涨+情绪≥6)已是足够克制的"让赢家跑"，不再放宽。
- caveat：入场是超集(三种退出在此集皆亏→真 alpha 在概念/龙头过滤，不在退出)；单一约400天区间。若日后重议：先补齐入场过滤 + 分区间稳健性再测。

### v1  2026-07-02  基线 + 上线"变更留痕"基础设施
- 改了什么：
  1. `db_cache.py` trade_log 表加 `sysver` 列（+幂等迁移 `_ensure_trade_log_columns`）；`add_trade` 支持 `sysver`。
  2. `trade_log.py` 加 `CURRENT_SYSVER='v1'` 常量、`buy --sysver`（默认自动打当前版本）、`calibration --by version`、open/history 显示 sysver。
  3. 3 笔已有持仓(华工科技/中晶科技/立昂微)回填 sysver='v1'。
- 为什么改：用户 2026-07-02 决定操作模式由"冻结一个月"改为"边走边优化"，需要一套让持续改动仍可复盘的地基。
- 期望：此后每笔交易带版本号，月末能按版本分段校准，区分某次优化到底是帮了还是害了。
- 类型：[中性] 基础设施，不碰任何选股/价位逻辑。

**v1 基线 = 什么状态（作为后续对比的原点）：**
- 选股：offline_screener V12——S1/S2/S3/S4 四策略；S4 死代码已修+复活、分档放宽追高；同行业/同概念**不去重**、全部按优先级排序展示（上限15）。
- 情绪/模式：market_mode(M1-M5) + market_sentiment 情绪周期(冰点/修复/发酵/高潮/分歧/退潮/过热)；退潮·过热→强制M5·0仓，高潮·分歧→×0.5仅A级。
- 风控：prompt.txt V11 尾部"价格区间执行·手动版"三条——追高上限+3%、分批止盈(压力位或+2~2.5%先卖一半、剩余移动止盈2.5%/S4为3%)、止损看收盘+盘中-6%熔断；D2/D3 强制清仓。

### Bug修复  2026-07-03  offline_screener 遇"行业为空"股票打印崩溃 → 补 None 防护
- 改了什么：`offline_screener.py` 5 处打印语句把 `c['industry'][:N]` / `c.get('industry','')[:N]` 改为 `(c.get('industry') or '')[:N]`——S1/S2/S3/S4 四张候选表(旧 line 799/1000/1183/1402)＋最终推荐表(旧 line 1490)。旧写法：当 key 存在但值为 `None` 时 `.get('industry','')` 返回 `None`（默认值只在 key 缺失时生效），`None[:8]` 直接 `TypeError` 崩溃。
- 为什么改：2026-07-03 尾盘选股跑 screener，S2 表打到第12行(sh.601156，行业=None)崩溃，S3/S4/最终推荐表整段打不出来。根因是内存记的「refresh 误删事故」遗留的 189 只行业分类为空的股票之一进了候选。
- 期望：候选表/推荐表完整打印，不再因个别行业空值中断；纯展示层，**不影响选股逻辑、评分、排序或选出哪些票**（崩溃发生在全部候选算完并排好序之后的打印阶段）。
- 类型：[Bug修复] 立即上。**不 bump CURRENT_SYSVER**（无任何交易行为/口径变化，仅防止显示崩溃）。
- 待办：这些股票行业分类为空的根，仍是 refresh 事故遗留的 189 只待 backfill（见内存 refresh误删事故），补齐行业后本防护仍保留兜底。

<!-- 下一条改动从这里往上加，记得先 bump trade_log.py 的 CURRENT_SYSVER -->
