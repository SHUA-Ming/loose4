# 项目结构规则

## 目录分层

所有长期保留的 Python 代码放在 `工具脚本/` 下，并按职责进入对应目录：

- `00_公共核心/`：数据库、行情接口、筹码计算、公共路径引导等跨流程复用能力。
- `10_数据更新/`：K 线、行业、指数、板块数据采集与增量更新。
- `20_市场环境/`：市场模式、情绪周期、指数环境、板块轮动判断。
- `30_候选短股/`：短线候选股扫描、实时选股、离线选股策略。
- `40_个股分析/`：单只股票或主题板块的复用型分析脚本。
- `50_交易记录/`：交易日志、持仓、胜率统计等记录工具。
- `60_候选ETF/`：候选 ETF 流程，后续新增 ETF 代码统一放这里。
- `70_长期持有股票/`：长期持有股票的筛选、跟踪、估值和复盘流程。

## 复用边界

公共能力优先放进 `00_公共核心/`，例如数据连接、行情标准化、通用指标、路径引导。候选短股、长期股、ETF 都会用到的逻辑，不要散落在策略脚本里。

跨目录 Python 脚本需要先接入 `00_公共核心/project_paths.py`，再导入本地模块，避免依赖当前运行目录。

## 一次性文件

一次性实验、临时诊断、单票临时深挖、旧版回测脚本，命名可临时使用 `_tmp_`、`_temp_`、`_check_`、`_backtest_`，但任务结束后必须删除。生成的 CSV/TXT 输出，如果没有进入正式复盘或体系文档，也要在任务结束时删除。

## 新增代码规则

新增代码前先判断归属目录；能复用公共能力就不复制粘贴。新增长期流程时同步补最小说明文档。完成任务后运行一次语法检查或轻量验证，并清理临时文件、缓存和输出产物。

## 数据库连接铁律（MySQL，必须遵守）

底层是 MySQL（`autocommit=False`，服务器 `lock_wait_timeout` 被设成 31536000≈365 天）。曾因此发生选股脚本"跑一小时不出结果"的事故：只读脚本的长事务持有元数据锁（MDL），把另一连接的 `CREATE/ALTER` DDL 卡死，而锁等待要等 365 天。根因与修复见下，违反任意一条都可能复现挂死。

1. **纯查询脚本（选股、分析、复盘、市场环境）一律用 `get_connection(readonly=True)`。** 只读连接开 autocommit，每条 SELECT 立即结束事务，不积累 MDL。这类脚本只读不写，没有事务需求。判定标准：脚本内没有 `INSERT/UPDATE/DELETE/REPLACE/commit` 就是纯查询脚本。

2. **禁止在分析/查询脚本里调用 `init_db()`。** `init_db()` 会执行 `CREATE TABLE` / `ALTER TABLE` 等 DDL，只属于 `10_数据更新/` 的采集入库脚本。分析脚本调它，等于在只读流程里塞 DDL，遇到长事务必然挂死。`analyze_concepts`、`market_mode`、`market_sentiment`、`concept_rotation`、`offline_screener` 都已移除内部 `init_db()`，新写分析脚本不要再加回来。

3. **禁止模块级（import 时）执行 `init_db()` 或建连接。** 一旦被别的脚本 import，就会在对方的事务上下文里触发 DDL。建连接/建表只能放在 `main()` 或函数内部、按需调用。

4. **写库脚本用默认 `get_connection()`（事务模式），但务必尽快 `commit()`，不要让事务长时间悬空。** 不要在一个事务里夹着对几千只股票的逐只查询又迟迟不提交。

5. 会话级护栏：`get_connection()` 已统一 `SET SESSION lock_wait_timeout=60`，任何锁等待最多 60 秒就报错，不再挂死。不要在脚本里把它改大。

6. 性能基线：全市场尾盘选股（`offline_screener.py`）正常约 3 分钟跑完。**超过 8 分钟即视为异常**，优先排查"是否误开长事务 / 是否在查询流程里触发了 DDL / 是否在等 MDL"，而不是盲目加大 `timeout`。加 `timeout` 只是掩盖，不是修复。
